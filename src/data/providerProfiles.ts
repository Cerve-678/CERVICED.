// Empty — all data is in Supabase
import { ProviderProfile } from '../types/providers';

export type { ServiceCategory, PortfolioItem, Service, Review, ProviderProfile } from '../types/providers';
export const providerProfiles: Record<string, ProviderProfile> = {};
